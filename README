prose.nvim
==========

Make writing in Neovim more pleasant

Commands
--------

Toggle prose

	:Prose

Setup
-----

	require("prose.nvim").setup({
		-- see spellfile.vim for list of spell files
	        lang = "en_us",
	})
